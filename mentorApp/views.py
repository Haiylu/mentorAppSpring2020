from django.shortcuts import render
from django.http import HttpResponse
from .models import mentorItem

# Create your views here.
#def mentorAppView(request):
       #return render(request, 'mentorApp.html',
           # {'all_items':all_mentor_items})      
#<form action="/addMentorApp/" method="post">{% csrf_token %}
   # <input type="text" name="content"/>
 #  # <input type="submit" value="Add"/>
#</form>